steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/${PROJECT_ID}/container2/container2:latest', '.']
  
  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/${PROJECT_ID}/container2/container2:latest']
  
  # Deploy container image to GKE
  - name: 'gcr.io/cloud-builders/kubectl'
    args:
      - 'set'
      - 'image'
      - 'deployment/container2'
      - 'container2=us-central1-docker.pkg.dev/${PROJECT_ID}/container2/container2:latest'
    env:
      - 'CLOUDSDK_COMPUTE_ZONE=us-central1-a'
      - 'CLOUDSDK_CONTAINER_CLUSTER=container2-cluster'

images:
  - 'us-central1-docker.pkg.dev/${PROJECT_ID}/container2/container2:latest'

options:
  logging: CLOUD_LOGGING_ONLY
